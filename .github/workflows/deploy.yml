github/workflows/deploy.yml‎
-51
Lines changed: 0 additions & 51 deletions
Original file line number	Diff line number	Diff line change
@@ -1,51 +0,0 @@
name: Build and Deploy NAF Hub
on:
  push:
    branches: ["main"]
  workflow_dispatch:
  schedule:
    - cron: '0 0 */2 * *' # Every 48 hours
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: "pages-${{ github.ref }}"
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      # FIX: Use 'npm install' instead of 'run: npm install' to auto-generate lock file if missing
      - name: Install Dependencies
        run: npm install
      - name: Fetch YouTube Data
        run: node scripts/fetch-youtube.js
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          CHANNEL_ID: UC41xXhw22o6Q2I2pTKB2kOg
      - name: Build App
        run: npm run build
      - name: Setup Pages
        uses: actions/configure-pages@v4
      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
